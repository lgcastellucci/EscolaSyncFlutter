# EscolaSync — Referência visual

- **`preview.html`** — abra no navegador pra ver os 4 estados da tela
  principal (não autenticado, autenticado, enviando, concluído), fiel
  ao código real em `lib/pages/home_page.dart` (mesmas cores, mesma
  estrutura de cards).
- **`screenshots/`** — capturas reais do app rodando em um Samsung S24,
  mostrando o visual novo (cabeçalho azul, cards claros, log técnico
  recolhível, versão no rodapé).
